{% extends 'base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}

    {{ form_start(renderForm) }}
    <button class="btn btn-primary" type="submit">Envoyer</button>
    {{ form_end(renderForm)}}
{% endblock %}
